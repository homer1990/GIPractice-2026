diff --git a/GIPractice.Wpf/ViewModels/Patients/PatientsPageViewModel.cs b/GIPractice.Wpf/ViewModels/Patients/PatientsPageViewModel.cs
index 5831bd3..9acdd9a 100644
--- a/GIPractice.Wpf/ViewModels/Patients/PatientsPageViewModel.cs
+++ b/GIPractice.Wpf/ViewModels/Patients/PatientsPageViewModel.cs
@@ -26,7 +26,16 @@ public sealed class PatientsPageViewModel : ViewModelBase
 
     private PatientVm? _selectedPatient;
     private bool _isSearchOpen;
-    private string? _searchText;
+	private string? _searchFirstName;
+	private string? _searchLastName;
+	private string? _searchFathersName;
+	private string? _searchPersonalNumber;
+	private string? _searchPhone;
+	private string? _searchEmail;
+	private bool _searchHasHadCA;
+	private bool _searchHasHadIBD;
+	private bool _searchHasPendingBiopsies;
+	private bool _searchHasScheduledEndo;
     private PatientVm? _searchSelectedPatient;
 
     public PatientVm? SelectedPatient
@@ -54,16 +63,18 @@ public sealed class PatientsPageViewModel : ViewModelBase
         set => SetProperty(ref _isSearchOpen, value);
     }
 
-    public string? SearchText
-    {
-        get => _searchText;
-        set
-        {
-            if (!SetProperty(ref _searchText, value))
-                return;
-            PerformSearch(); // live filter
-        }
-    }
+	// Explicit field-by-field search (no live filtering for performance).
+	public string? SearchFirstName { get => _searchFirstName; set => SetProperty(ref _searchFirstName, value); }
+	public string? SearchLastName { get => _searchLastName; set => SetProperty(ref _searchLastName, value); }
+	public string? SearchFathersName { get => _searchFathersName; set => SetProperty(ref _searchFathersName, value); }
+	public string? SearchPersonalNumber { get => _searchPersonalNumber; set => SetProperty(ref _searchPersonalNumber, value); }
+	public string? SearchPhone { get => _searchPhone; set => SetProperty(ref _searchPhone, value); }
+	public string? SearchEmail { get => _searchEmail; set => SetProperty(ref _searchEmail, value); }
+
+	public bool SearchHasHadCA { get => _searchHasHadCA; set => SetProperty(ref _searchHasHadCA, value); }
+	public bool SearchHasHadIBD { get => _searchHasHadIBD; set => SetProperty(ref _searchHasHadIBD, value); }
+	public bool SearchHasPendingBiopsies { get => _searchHasPendingBiopsies; set => SetProperty(ref _searchHasPendingBiopsies, value); }
+	public bool SearchHasScheduledEndo { get => _searchHasScheduledEndo; set => SetProperty(ref _searchHasScheduledEndo, value); }
 
     public ObservableCollection<PatientVm> AllPatients { get; } = new();
     public ObservableCollection<PatientVm> SearchResults { get; } = new();
@@ -97,6 +108,7 @@ public sealed class PatientsPageViewModel : ViewModelBase
     // Commands
     public RelayCommand ToggleSearchCommand { get; }
     public RelayCommand PerformSearchCommand { get; }
+	public RelayCommand ClearSearchCommand { get; }
     public RelayCommand SelectSearchPatientCommand { get; }
 
     public RelayCommand NewPatientCommand { get; }
@@ -107,6 +119,9 @@ public sealed class PatientsPageViewModel : ViewModelBase
     public RelayCommand PhotoFromFileCommand { get; }
     public RelayCommand RefreshPatientCommand { get; } // ΕΠΑΝΑΦΟΡΑ: reset photo baseline
 
+	public RelayCommand<AppointmentVm> EditPatientAppointmentCommand { get; }
+	public RelayCommand<AppointmentVm> DeletePatientAppointmentCommand { get; }
+
     // fields
     private readonly RelayCommand _newAppointmentCommand;
     private readonly RelayCommand _closeSchedulerCommand;
@@ -119,6 +134,7 @@ public sealed class PatientsPageViewModel : ViewModelBase
     {
         ToggleSearchCommand = new RelayCommand(() => IsSearchOpen = !IsSearchOpen);
         PerformSearchCommand = new RelayCommand(PerformSearch);
+		ClearSearchCommand = new RelayCommand(ClearSearch);
         SelectSearchPatientCommand = new RelayCommand(SelectSearchPatient, () => SearchSelectedPatient != null);
 
         NewPatientCommand = new RelayCommand(NewPatient);
@@ -132,6 +148,9 @@ public sealed class PatientsPageViewModel : ViewModelBase
             () => SelectedPatient?.ResetPhotoToBaseline(),
             () => SelectedPatient != null && SelectedPatient.IsPhotoDirty);
 
+		EditPatientAppointmentCommand = new RelayCommand<AppointmentVm>(EditPatientAppointment);
+		DeletePatientAppointmentCommand = new RelayCommand<AppointmentVm>(DeletePatientAppointment);
+
         _newAppointmentCommand = new RelayCommand(OpenScheduler, () => SelectedPatient != null);
         _closeSchedulerCommand = new RelayCommand(() => IsSchedulerOpen = false);
 
@@ -155,10 +174,13 @@ public sealed class PatientsPageViewModel : ViewModelBase
 
         var p1 = PatientVm.CreateNew();
         p1.Id = 1; p1.FirstName = "Γιώργος"; p1.LastName = "Παπαδόπουλος"; p1.PhoneNumber = "69xxxxxxxx";
+		p1.HasHadIBD = true;
         p1.AcceptPhotoAsBaseline();
 
         var p2 = PatientVm.CreateNew();
         p2.Id = 2; p2.FirstName = "Μαρία"; p2.LastName = "Ιωάννου"; p2.PhoneNumber = "69yyyyyyyy";
+		p2.HasPendingBiopsies = true;
+		p2.HasScheduledEndo = true;
         p2.AcceptPhotoAsBaseline();
 
         AllPatients.Add(p1);
@@ -169,14 +191,31 @@ public sealed class PatientsPageViewModel : ViewModelBase
     {
         SearchResults.Clear();
 
-        var q = (SearchText ?? "").Trim();
-        var src = string.IsNullOrWhiteSpace(q)
-            ? AllPatients
-            : new ObservableCollection<PatientVm>(
-                AllPatients.Where(p =>
-                    (p.FirstName ?? "").Contains(q, StringComparison.OrdinalIgnoreCase) ||
-                    (p.LastName ?? "").Contains(q, StringComparison.OrdinalIgnoreCase) ||
-                    (p.PhoneNumber ?? "").Contains(q, StringComparison.OrdinalIgnoreCase)));
+		string norm(string? s) => (s ?? "").Trim();
+		bool has(string? s) => !string.IsNullOrWhiteSpace(s);
+		bool contains(string? hay, string needle)
+			=> (hay ?? "").Contains(needle, StringComparison.OrdinalIgnoreCase);
+
+		var fn = norm(SearchFirstName);
+		var ln = norm(SearchLastName);
+		var fat = norm(SearchFathersName);
+		var pn = norm(SearchPersonalNumber);
+		var ph = norm(SearchPhone);
+		var em = norm(SearchEmail);
+
+		var src = new ObservableCollection<PatientVm>(
+			AllPatients.Where(p =>
+				(!has(fn) || contains(p.FirstName, fn)) &&
+				(!has(ln) || contains(p.LastName, ln)) &&
+				(!has(fat) || contains(p.FathersName, fat)) &&
+				(!has(pn) || contains(p.PersonalNumber, pn)) &&
+				(!has(ph) || contains(p.PhoneNumber, ph)) &&
+				(!has(em) || contains(p.Email, em)) &&
+				(!SearchHasHadCA || p.HasHadCA) &&
+				(!SearchHasHadIBD || p.HasHadIBD) &&
+				(!SearchHasPendingBiopsies || p.HasPendingBiopsies) &&
+				(!SearchHasScheduledEndo || p.HasScheduledEndo)
+			));
 
         foreach (var p in src)
             SearchResults.Add(p);
@@ -184,6 +223,21 @@ public sealed class PatientsPageViewModel : ViewModelBase
         SearchSelectedPatient = SearchResults.FirstOrDefault();
     }
 
+	private void ClearSearch()
+	{
+		SearchFirstName = null;
+		SearchLastName = null;
+		SearchFathersName = null;
+		SearchPersonalNumber = null;
+		SearchPhone = null;
+		SearchEmail = null;
+		SearchHasHadCA = false;
+		SearchHasHadIBD = false;
+		SearchHasPendingBiopsies = false;
+		SearchHasScheduledEndo = false;
+		PerformSearch();
+	}
+
     private void SelectSearchPatient()
     {
         if (SearchSelectedPatient == null) return;
@@ -290,6 +344,18 @@ public sealed class PatientsPageViewModel : ViewModelBase
         IsSchedulerOpen = true;
     }
 
+	private void EditPatientAppointment(AppointmentVm appt)
+	{
+		// Dummy implementation for now.
+		// Later: open scheduler/details with this appointment pre-selected.
+		IsSchedulerOpen = true;
+	}
+
+	private void DeletePatientAppointment(AppointmentVm appt)
+	{
+		PatientAppointments.Remove(appt);
+	}
+
     private void RaiseAllCanExecutes()
     {
         SavePatientCommand.RaiseCanExecuteChanged();
diff --git a/GIPractice.Wpf/ViewModels/Various.cs b/GIPractice.Wpf/ViewModels/Various.cs
index c75e5ab..c09fdec 100644
--- a/GIPractice.Wpf/ViewModels/Various.cs
+++ b/GIPractice.Wpf/ViewModels/Various.cs
@@ -35,6 +35,13 @@ public sealed class PatientVm : ViewModelBase
     public string? PhoneNumber { get; set; }
     public string? Address { get; set; }
 
+    // Temporary flags for search UX scaffolding.
+    // Later these should be computed from real domain data (diagnoses, pathology workflow, schedules, etc.).
+    public bool HasHadCA { get; set; }
+    public bool HasHadIBD { get; set; }
+    public bool HasPendingBiopsies { get; set; }
+    public bool HasScheduledEndo { get; set; }
+
     private ImageSource? _photoImageSource;
     public ImageSource? PhotoImageSource
     {
diff --git a/GIPractice.Wpf/Views/Templates/PatientsPageTemplate.xaml b/GIPractice.Wpf/Views/Templates/PatientsPageTemplate.xaml
index 24d1f07..c264ac7 100644
--- a/GIPractice.Wpf/Views/Templates/PatientsPageTemplate.xaml
+++ b/GIPractice.Wpf/Views/Templates/PatientsPageTemplate.xaml
@@ -2,98 +2,151 @@
     xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
     xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
     xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
+    xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
     xmlns:vm="clr-namespace:GIPractice.Wpf.ViewModels">
 
     <BooleanToVisibilityConverter x:Key="BoolToVis"/>
 
+    <!-- Small, uniform icon buttons for toolbars (prevents DockPanel "last child fills" surprises) -->
+    <Style x:Key="ToolbarIconButton" TargetType="Button">
+        <Setter Property="Width" Value="44"/>
+        <Setter Property="Height" Value="44"/>
+        <Setter Property="Padding" Value="0"/>
+        <Setter Property="Margin" Value="5"/>
+        <Setter Property="HorizontalAlignment" Value="Left"/>
+        <Setter Property="VerticalAlignment" Value="Top"/>
+    </Style>
+
+    <!-- Trimming helper for DataGrid text columns -->
+    <Style x:Key="DataGridTrimText" TargetType="TextBlock">
+        <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
+        <Setter Property="TextWrapping" Value="NoWrap"/>
+        <Setter Property="VerticalAlignment" Value="Center"/>
+    </Style>
+
     <DataTemplate DataType="{x:Type vm:PatientsPageViewModel}">
         <Grid>
 
             <Grid.ColumnDefinitions>
-                <!-- Search panel column -->
-                <ColumnDefinition Width="420"/>
+                <!-- Search panel column: Auto so Collapsed search doesn't keep empty space -->
+                <ColumnDefinition Width="Auto"/>
                 <ColumnDefinition Width="*"/>
             </Grid.ColumnDefinitions>
 
             <!-- LEFT: SEARCH PANEL -->
-            <Border Grid.Column="0"
-                    Background="{DynamicResource MahApps.Brushes.Gray10}"
-                    BorderBrush="{DynamicResource MahApps.Brushes.Gray7}"
-                    BorderThickness="0,0,1,0"
-                    Visibility="{Binding IsSearchOpen, Converter={StaticResource BoolToVis}}">
-
-                <DockPanel Margin="10">
-
-                    <DockPanel DockPanel.Dock="Top" LastChildFill="False">
-                        <TextBlock FontWeight="Bold" FontSize="16"
-                                   VerticalAlignment="Center"
-                                   Text="ΑΝΑΖΗΤΗΣΗ ΑΣΘΕΝΟΥΣ"/>
-                        <Button DockPanel.Dock="Right"
-                                Padding="8,4"
-                                Command="{Binding ToggleSearchCommand}">
-                            <TextBlock FontFamily="Segoe Fluent Icons" FontSize="16">&#xE711;</TextBlock>
-                        </Button>
-                    </DockPanel>
-
-                    <TextBox Margin="0,10,0,8"
-                             mah:TextBoxHelper.Watermark="Όνομα / Επίθετο / Τηλ."
-                             Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>
-
-                    <Button Margin="0,0,0,10"
-                            Content="ΑΝΑΖΗΤΗΣΗ"
-                            Command="{Binding PerformSearchCommand}"/>
-
-                    <ListBox ItemsSource="{Binding SearchResults}"
-                             SelectedItem="{Binding SearchSelectedPatient, Mode=TwoWay}">
-                        <ListBox.ItemTemplate>
-                            <DataTemplate>
-                                <StackPanel Margin="4">
-                                    <TextBlock FontWeight="SemiBold">
-                                        <Run Text="{Binding LastName}"/><Run Text=" "/><Run Text="{Binding FirstName}"/>
-                                    </TextBlock>
-                                    <TextBlock Opacity="0.75" Text="{Binding PhoneNumber}"/>
-                                </StackPanel>
-                            </DataTemplate>
-                        </ListBox.ItemTemplate>
-                    </ListBox>
-
-                    <Button Margin="0,10,0,0"
-                            Content="ΕΠΙΛΟΓΗ"
-                            Command="{Binding SelectSearchPatientCommand}"/>
-                </DockPanel>
-            </Border>
+			    <Border Grid.Column="0" Width="420"
+			            Background="{DynamicResource MahApps.Brushes.Gray10}"
+			            BorderBrush="{DynamicResource MahApps.Brushes.Gray7}"
+			            BorderThickness="0,0,1,0"
+			            Visibility="{Binding IsSearchOpen, Converter={StaticResource BoolToVis}}">
+
+			        <Grid Margin="10">
+			            <Grid.RowDefinitions>
+			                <RowDefinition Height="Auto"/>
+			                <RowDefinition Height="Auto"/>
+			                <RowDefinition Height="*"/>
+			                <RowDefinition Height="Auto"/>
+			            </Grid.RowDefinitions>
+
+			            <!-- Header + close -->
+			            <DockPanel Grid.Row="0" LastChildFill="False">
+			                <TextBlock FontWeight="Bold" FontSize="16"
+			                           VerticalAlignment="Center"
+			                           Text="ΑΝΑΖΗΤΗΣΗ ΑΣΘΕΝΟΥΣ"/>
+			                <Button DockPanel.Dock="Right" Padding="8,4"
+			                        Command="{Binding ToggleSearchCommand}">
+			                    <TextBlock FontFamily="Segoe Fluent Icons" FontSize="16">&#xE711;</TextBlock>
+			                </Button>
+			            </DockPanel>
+
+			            <!-- Criteria -->
+			            <ScrollViewer Grid.Row="1" Margin="0,10,0,10"
+			                          VerticalScrollBarVisibility="Auto"
+			                          HorizontalScrollBarVisibility="Disabled">
+			                <StackPanel>
+			                    <TextBox Margin="0,0,0,6" mah:TextBoxHelper.Watermark="Όνομα"
+			                             Text="{Binding SearchFirstName, UpdateSourceTrigger=PropertyChanged}"/>
+			                    <TextBox Margin="0,0,0,6" mah:TextBoxHelper.Watermark="Επίθετο"
+			                             Text="{Binding SearchLastName, UpdateSourceTrigger=PropertyChanged}"/>
+			                    <TextBox Margin="0,0,0,6" mah:TextBoxHelper.Watermark="Πατρώνυμο"
+			                             Text="{Binding SearchFathersName, UpdateSourceTrigger=PropertyChanged}"/>
+			                    <TextBox Margin="0,0,0,6" mah:TextBoxHelper.Watermark="ΠΑ"
+			                             Text="{Binding SearchPersonalNumber, UpdateSourceTrigger=PropertyChanged}"/>
+			                    <TextBox Margin="0,0,0,6" mah:TextBoxHelper.Watermark="Τηλέφωνο"
+			                             Text="{Binding SearchPhone, UpdateSourceTrigger=PropertyChanged}"/>
+			                    <TextBox Margin="0,0,0,10" mah:TextBoxHelper.Watermark="Email"
+			                             Text="{Binding SearchEmail, UpdateSourceTrigger=PropertyChanged}"/>
+
+			                    <Expander Header="Φίλτρα" IsExpanded="False">
+			                        <StackPanel Margin="0,6,0,0">
+			                            <CheckBox Content="Ιστορικό CA" IsChecked="{Binding SearchHasHadCA}"/>
+			                            <CheckBox Content="Ιστορικό IBD" IsChecked="{Binding SearchHasHadIBD}"/>
+			                            <CheckBox Content="Έχει εκκρεμείς βιοψίες" IsChecked="{Binding SearchHasPendingBiopsies}"/>
+			                            <CheckBox Content="Έχει προγραμματισμένη ενδοσκόπηση" IsChecked="{Binding SearchHasScheduledEndo}"/>
+			                        </StackPanel>
+			                    </Expander>
+
+			                    <UniformGrid Columns="2" Margin="0,10,0,0">
+			                        <Button Content="ΑΝΑΖΗΤΗΣΗ" Margin="0,0,6,0"
+			                                Command="{Binding PerformSearchCommand}"/>
+			                        <Button Content="ΚΑΘΑΡΙΣΜΑ" Margin="6,0,0,0"
+			                                Command="{Binding ClearSearchCommand}"/>
+			                    </UniformGrid>
+			                </StackPanel>
+			            </ScrollViewer>
+
+			            <!-- Results -->
+			            <ListBox Grid.Row="2" ItemsSource="{Binding SearchResults}"
+			                     SelectedItem="{Binding SearchSelectedPatient, Mode=TwoWay}">
+			                <ListBox.ItemTemplate>
+			                    <DataTemplate>
+			                        <StackPanel Margin="4">
+			                            <TextBlock FontWeight="SemiBold">
+			                                <Run Text="{Binding LastName}"/><Run Text=" "/><Run Text="{Binding FirstName}"/>
+			                            </TextBlock>
+			                            <TextBlock Opacity="0.75" Text="{Binding PhoneNumber}"/>
+			                        </StackPanel>
+			                    </DataTemplate>
+			                </ListBox.ItemTemplate>
+			            </ListBox>
+
+			            <Button Grid.Row="3" Margin="0,10,0,0"
+			                    Content="ΕΠΙΛΟΓΗ"
+			                    Command="{Binding SelectSearchPatientCommand}"/>
+			        </Grid>
+			    </Border>
 
             <!-- RIGHT: MAIN CONTENT -->
             <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto">
                 <StackPanel Margin="5" Orientation="Vertical">
 
-                    <!-- Toolbar -->
-                    <DockPanel Margin="0,0,0,8">
-                        <Button Width="Auto" Height="Auto" Padding="10" Margin="5"
-                                ToolTip="ΑΝΑΖΗΤΗΣΗ ΑΣΘΕΝΟΥΣ"
-                                Command="{Binding ToggleSearchCommand}">
-                            <TextBlock FontFamily="Segoe Fluent Icons" FontSize="24">&#xE721;</TextBlock>
-                        </Button>
-
-                        <Button Width="Auto" Height="Auto" Padding="10" Margin="5"
-                                ToolTip="ΝΕΟΣ ΑΣΘΕΝΗΣ"
-                                Command="{Binding NewPatientCommand}">
-                            <TextBlock FontFamily="Segoe Fluent Icons" FontSize="24">&#xE8FA;</TextBlock>
-                        </Button>
-
-                        <Button Width="Auto" Height="Auto" Padding="10" Margin="5"
-                                ToolTip="ΕΝΗΜΕΡΩΣΗ ΑΣΘΕΝΟΥΣ"
-                                Command="{Binding SavePatientCommand}">
-                            <TextBlock FontFamily="Segoe Fluent Icons" FontSize="24">&#xE74E;</TextBlock>
-                        </Button>
-
-                        <Button Width="Auto" Height="Auto" Padding="10" Margin="5"
-                                ToolTip="ΔΙΑΓΡΑΦΗ ΑΣΘΕΝΟΥΣ"
-                                Background="Red" Foreground="Yellow"
-                                Command="{Binding DeletePatientCommand}">
-                            <TextBlock FontFamily="Segoe Fluent Icons" FontSize="24">&#xE74D;</TextBlock>
-                        </Button>
-                    </DockPanel>
+			            <!-- Toolbar (StackPanel so the last button doesn't stretch to fill remaining width) -->
+			            <StackPanel Margin="0,0,0,8" Orientation="Horizontal">
+			                <Button Style="{StaticResource ToolbarIconButton}"
+			                        ToolTip="ΑΝΑΖΗΤΗΣΗ ΑΣΘΕΝΟΥΣ"
+			                        Command="{Binding ToggleSearchCommand}">
+			                    <TextBlock FontFamily="Segoe Fluent Icons" FontSize="24">&#xE721;</TextBlock>
+			                </Button>
+
+			                <Button Style="{StaticResource ToolbarIconButton}"
+			                        ToolTip="ΝΕΟΣ ΑΣΘΕΝΗΣ"
+			                        Command="{Binding NewPatientCommand}">
+			                    <TextBlock FontFamily="Segoe Fluent Icons" FontSize="24">&#xE8FA;</TextBlock>
+			                </Button>
+
+			                <Button Style="{StaticResource ToolbarIconButton}"
+			                        ToolTip="ΕΝΗΜΕΡΩΣΗ ΑΣΘΕΝΟΥΣ"
+			                        Command="{Binding SavePatientCommand}">
+			                    <TextBlock FontFamily="Segoe Fluent Icons" FontSize="24">&#xE74E;</TextBlock>
+			                </Button>
+
+			                <Button Style="{StaticResource ToolbarIconButton}"
+			                        ToolTip="ΔΙΑΓΡΑΦΗ ΑΣΘΕΝΟΥΣ"
+			                        Background="Red" Foreground="Yellow"
+			                        Command="{Binding DeletePatientCommand}">
+			                    <TextBlock FontFamily="Segoe Fluent Icons" FontSize="24">&#xE74D;</TextBlock>
+			                </Button>
+			            </StackPanel>
 
                     <!-- Patient details section (same bindings as before) -->
                     <StackPanel Width="1000">
@@ -219,18 +272,24 @@
                                         </DataGrid>
                                     </GroupBox>
 
-                                    <GroupBox Margin="0,10,20,0" Header="ΠΡΟΣΕΧΗ ΡΑΝΤΕΒΟΥ">
-                                        <DataGrid AutoGenerateColumns="False" IsReadOnly="True"
-                                                  CanUserAddRows="False" CanUserDeleteRows="False"
-                                                  ItemsSource="{Binding PendingAppointments}">
-                                            <DataGrid.Columns>
-                                                <DataGridTextColumn Header="ΗΜΕΡΟΜΗΝΙΑ" Binding="{Binding Date, StringFormat={}{0:dd/MM/yyyy}}" Width="*"/>
-                                                <DataGridTextColumn Header="ΩΡΑ" Binding="{Binding Date, StringFormat={}{0:HH:mm}}" Width="0.5*"/>
-                                                <DataGridCheckBoxColumn Header="ΕΠΕΙΓΟΝ" Binding="{Binding IsUrgent}" Width="0.7*"/>
-                                                <DataGridTextColumn Header="ΤΥΠΟΣ" Binding="{Binding AppointmentTypeName}" Width="0.85*"/>
-                                            </DataGrid.Columns>
-                                        </DataGrid>
-                                    </GroupBox>
+							            <GroupBox Margin="0,10,20,0" Header="ΠΡΟΣΕΧΗ ΡΑΝΤΕΒΟΥ">
+							                <DataGrid AutoGenerateColumns="False" IsReadOnly="True"
+							                          CanUserAddRows="False" CanUserDeleteRows="False"
+							                          ItemsSource="{Binding PendingAppointments}">
+							                    <DataGrid.Columns>
+							                        <DataGridTextColumn Header="ΗΜΕΡΟΜΗΝΙΑ" Binding="{Binding Date, StringFormat={}{0:dd/MM}}" Width="0.9*"/>
+							                        <DataGridTextColumn Header="ΩΡΑ" Binding="{Binding Date, StringFormat={}{0:HH:mm}}" Width="0.4*"/>
+							                        <DataGridCheckBoxColumn Header="ΕΠΕΙΓΟΝ" Binding="{Binding IsUrgent}" Width="0.4*"/>
+							                        <DataGridTextColumn Header="ΤΥΠΟΣ" Binding="{Binding AppointmentTypeName}" Width="1.3*">
+							                            <DataGridTextColumn.ElementStyle>
+							                                <Style TargetType="TextBlock" BasedOn="{StaticResource DataGridTrimText}">
+							                                    <Setter Property="ToolTip" Value="{Binding AppointmentTypeName}"/>
+							                                </Style>
+							                            </DataGridTextColumn.ElementStyle>
+							                        </DataGridTextColumn>
+							                    </DataGrid.Columns>
+							                </DataGrid>
+							            </GroupBox>
                                 </StackPanel>
 
                             </StackPanel>
@@ -241,20 +300,46 @@
                     <StackPanel Width="1000">
                         <GroupBox Margin="0,10,20,0" Header="ΡΑΝΤΕΒΟΥ ΑΣΘΕΝΟΥΣ">
                             <StackPanel>
-                                <DataGrid AutoGenerateColumns="False" IsReadOnly="True"
-                                          CanUserAddRows="False" CanUserDeleteRows="False"
-                                          ItemsSource="{Binding PatientAppointments}">
-                                    <DataGrid.Columns>
-                                        <DataGridTextColumn Header="ΗΜΕΡΟΜΗΝΙΑ" Binding="{Binding Start, StringFormat={}{0:dd/MM/yyyy}}" Width="*"/>
-                                        <DataGridTextColumn Header="ΩΡΑ" Binding="{Binding Start, StringFormat={}{0:HH:mm}}" Width="*"/>
-                                        <DataGridTextColumn Header="ΣΚΟΠΟΣ" Binding="{Binding AppointmentTypeName}" Width="*"/>
-                                    </DataGrid.Columns>
-                                </DataGrid>
-
-                                <Button Margin="5" ToolTip="ΝΕΟ ΡΑΝΤΕΒΟΥ"
-                                        Command="{Binding NewAppointmentCommand}">
-                                    <TextBlock FontFamily="Segoe Fluent Icons" FontSize="24">&#xE710;</TextBlock>
-                                </Button>
+							                <DataGrid AutoGenerateColumns="False" IsReadOnly="True"
+							                          CanUserAddRows="False" CanUserDeleteRows="False"
+							                          ItemsSource="{Binding PatientAppointments}">
+							                    <DataGrid.Columns>
+							                        <DataGridTextColumn Header="ΗΜΕΡΟΜΗΝΙΑ" Binding="{Binding Start, StringFormat={}{0:dd/MM/yyyy}}" Width="*"/>
+							                        <DataGridTextColumn Header="ΩΡΑ" Binding="{Binding Start, StringFormat={}{0:HH:mm}}" Width="*"/>
+							                        <DataGridTextColumn Header="ΣΚΟΠΟΣ" Binding="{Binding AppointmentTypeName}" Width="2*" MinWidth="200">
+							                            <DataGridTextColumn.ElementStyle>
+							                                <Style TargetType="TextBlock" BasedOn="{StaticResource DataGridTrimText}">
+							                                    <Setter Property="ToolTip" Value="{Binding AppointmentTypeName}"/>
+							                                </Style>
+							                            </DataGridTextColumn.ElementStyle>
+							                        </DataGridTextColumn>
+
+							                        <!-- Edit/Delete icons on each row -->
+							                        <DataGridTemplateColumn Width="80" IsReadOnly="True">
+							                            <DataGridTemplateColumn.CellTemplate>
+							                                <DataTemplate>
+							                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
+							                                        <Button Padding="6" Margin="0,0,6,0" ToolTip="Επεξεργασία"
+							                                                Command="{Binding DataContext.EditPatientAppointmentCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
+							                                                CommandParameter="{Binding}">
+							                                            <iconPacks:PackIconMaterial Kind="Pencil" Width="18" Height="18"/>
+							                                        </Button>
+							                                        <Button Padding="6" ToolTip="Διαγραφή"
+							                                                Command="{Binding DataContext.DeletePatientAppointmentCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
+							                                                CommandParameter="{Binding}">
+							                                            <iconPacks:PackIconMaterial Kind="TrashCanOutline" Width="18" Height="18"/>
+							                                        </Button>
+							                                    </StackPanel>
+							                                </DataTemplate>
+							                            </DataGridTemplateColumn.CellTemplate>
+							                        </DataGridTemplateColumn>
+							                    </DataGrid.Columns>
+							                </DataGrid>
+
+							                <Button Style="{StaticResource ToolbarIconButton}" ToolTip="ΝΕΟ ΡΑΝΤΕΒΟΥ"
+							                        Command="{Binding NewAppointmentCommand}">
+							                    <TextBlock FontFamily="Segoe Fluent Icons" FontSize="24">&#xE710;</TextBlock>
+							                </Button>
                             </StackPanel>
                         </GroupBox>
                     </StackPanel>
