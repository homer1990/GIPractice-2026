<UserControl x:Class="GIPractice.Wpf.Views.Patients.PatientCardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:loc="clr-namespace:GIPractice.Wpf.Localization">

    <!-- DataContext: PatientListItemDto (from API) -->

    <Grid>
        <Grid.ColumnDefinitions>
            <!-- Photo -->
            <ColumnDefinition Width="120" />
            <!-- Text info -->
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Photo placeholder (wire to real photo later) -->
        <Border Grid.Column="0"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                Width="96"
                Height="96"
                CornerRadius="4"
                BorderBrush="{DynamicResource Brush.Border}"
                BorderThickness="1"
                Background="{DynamicResource Brush.WindowBackground}">
            <TextBlock Text="{loc:Loc Table=Dashboard Field=PhotoPlaceholder}"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       Opacity="0.6" />
        </Border>

        <!-- Basic patient info -->
        <StackPanel Grid.Column="1"
                    Orientation="Vertical"
                    Margin="8,0,0,0">
            <TextBlock FontWeight="Bold"
                       FontSize="16">
                <Run Text="{Binding LastName}" />
                <Run Text=" " />
                <Run Text="{Binding FirstName}" />
            </TextBlock>

            <TextBlock Text="{Binding PersonalNumber}" />
            <TextBlock Text="{Binding BirthDay, StringFormat='Birth: {0:yyyy-MM-dd}'}" />
            <TextBlock Text="{Binding PhoneNumber}" />
            <TextBlock Text="{Binding Email}" />
        </StackPanel>
    </Grid>
</UserControl>
