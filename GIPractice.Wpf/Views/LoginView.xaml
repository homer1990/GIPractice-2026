<UserControl x:Class="GIPractice.Wpf.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="40">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Text="{Binding Title}" FontSize="20" FontWeight="Bold" />
        <StackPanel Grid.Row="1" Margin="0,12,0,0">
            <TextBlock Text="{Binding ServerUrlLabel}" />
            <TextBox Text="{Binding ServerUrl, UpdateSourceTrigger=PropertyChanged}" />
        </StackPanel>
        <StackPanel Grid.Row="2" Margin="0,12,0,0">
            <TextBlock Text="{Binding UserNameLabel}" />
            <TextBox Text="{Binding UserName, UpdateSourceTrigger=PropertyChanged}" />
            <TextBlock Text="{Binding PasswordLabel}" />
            <PasswordBox x:Name="PasswordBox" PasswordChanged="OnPasswordChanged" />
        </StackPanel>
        <Button Grid.Row="3" Content="{Binding SubmitLabel}" Command="{Binding SignInCommand}" />
        <TextBlock Grid.Row="4" Text="{Binding Status}" Margin="0,8,0,0" />
    </Grid>
</UserControl>
