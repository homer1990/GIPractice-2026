<UserControl x:Class="GIPractice.Wpf.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="Dashboard" FontSize="20" FontWeight="Bold" Padding="20"/>
        <ListBox Grid.Row="1" ItemsSource="{Binding ActivityLog}" />
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Record activity" Command="{Binding RefreshCommand}" />
            <TextBlock Margin="12,0,0,0" Text="{Binding Status}" VerticalAlignment="Center" />
        </StackPanel>
    </Grid>
</UserControl>
