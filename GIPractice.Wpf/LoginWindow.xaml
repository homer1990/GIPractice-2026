<Window x:Class="GIPractice.Wpf.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:loc="clr-namespace:GIPractice.Wpf.Localization"
        Title="{loc:Loc Table=Login Field=Title}"
        Height="220"
        Width="360"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="{DynamicResource Brush.WindowBackground}"
        Foreground="{DynamicResource Brush.WindowForeground}">
    <Grid Margin="16"
          Background="{DynamicResource Brush.PanelBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="8"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="8"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="8"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="8"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0"
                   Style="{StaticResource FormLabel}"
                   Text="{loc:Loc Table=Login Field=Username}"/>
        <TextBox Grid.Row="0" Grid.Column="2"
                 MinWidth="180"
                 Text="{Binding UserName, UpdateSourceTrigger=PropertyChanged}" />

        <TextBlock Grid.Row="2" Grid.Column="0"
                   Style="{StaticResource FormLabel}"
                   Text="{loc:Loc Table=Login Field=Password}"/>
        <!-- Using TextBox for now; we can replace with PasswordBox binding later -->
        <TextBox Grid.Row="2" Grid.Column="2"
                 MinWidth="180"
                 Margin="0,0,0,4"
                 Text="{Binding Password, UpdateSourceTrigger=PropertyChanged}"
                 />
        <!-- If you really want a PasswordBox now, we can wire it separately. -->

        <TextBlock Grid.Row="4" Grid.ColumnSpan="3"
                   Foreground="{DynamicResource Brush.ErrorText}"
                   TextWrapping="Wrap"
                   Text="{Binding ErrorMessage}" />

        <StackPanel Grid.Row="6" Grid.ColumnSpan="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="{loc:Loc Table=Login Field=Button}"
                    Padding="16,4"
                    IsDefault="True"
                    Command="{Binding LoginCommand}" />

        </StackPanel>
    </Grid>
</Window>