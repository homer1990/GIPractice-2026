<UserControl x:Class="GIPractice.Wpf.Views.RescheduleTasksDetailsView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,10">
            <Button Content="Save" Command="{Binding SaveCommand}" />
            <Button Margin="10,0,0,0" Content="Delete" Command="{Binding DeleteCommand}" />
            <Button Margin="10,0,0,0" Content="Close" Command="{Binding CloseCommand}" />
        </DockPanel>

        <TextBlock Grid.Row="1"
                   Text="TODO: RescheduleTasks editor UI"
                   FontSize="18"
                   VerticalAlignment="Center"
                   HorizontalAlignment="Center"
                   Opacity="0.7"/>
    </Grid>
</UserControl>
